@inherits LayoutComponentBase
@using Pet.Jira.Infrastructure.Jira
@inject IJiraService _jiraService

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout Style="height: 100vh">
    <MudAppBar Elevation="1">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" OnClick="@ToggleDrawer" />
        <MudText Typo="Typo.h5" Class="ml-3">Jira workflow</MudText>
        <MudIconButton Icon="@Icons.Custom.Brands.GitHub" Color="Color.Inherit" />
        <MudSpacer></MudSpacer>
        <AuthorizeView>
            <Authorized>
                <img src="@avatar"/>
                <MudElement Class="mr-2 ml-2">@context.User.Identity.Name</MudElement>
                <MudLink Href="/logout" Class="pet-white" Color="Color.Inherit"><MudIcon Icon="@Icons.Filled.Logout"/></MudLink>
            </Authorized>
            <NotAuthorized>
                <MudLink Href="/login" Class="pet-white" Color="Color.Inherit"><MudIcon Icon="@Icons.Filled.Login" /></MudLink>
            </NotAuthorized>
        </AuthorizeView>
    </MudAppBar>
    <MudDrawer @bind-Open="@_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
        <NavMenu />
    </MudDrawer>
    <MudMainContent Style="height: 100vh">
        <AuthorizeView>
            <Authorized>
                <MudContainer Class="mt-6" MaxWidth="MaxWidth.ExtraLarge">
                    @Body
                </MudContainer>
            </Authorized>
            <NotAuthorized>
                <MudContainer Class="mt-6" MaxWidth="MaxWidth.Medium">
                    <Login></Login>
                </MudContainer>
            </NotAuthorized>
        </AuthorizeView>
    </MudMainContent>
</MudLayout>


@code{
    bool _drawerOpen = true;
    public string avatar = "images/default_user_icon_16.png";

    void ToggleDrawer()
    {
        _drawerOpen = !_drawerOpen;
    }

    //protected override async Task OnInitializedAsync()
    //{
    //    var jiraAvatar = await _jiraService.GetCurrentUserAvatar();
    //    avatar = string.IsNullOrEmpty(jiraAvatar)
    //        ? "images/default_user_icon_16.png"
    //        : jiraAvatar;
    //}
}